#!/usr/bin/env bash
# TODO: Implement QVS syntax validation hook
# This script receives JSON on stdin with details about the Write tool invocation.
# It should validate .qvs files for basic syntax correctness.
# Exit 0 for success, non-zero to signal issues (output goes to Claude's context).
