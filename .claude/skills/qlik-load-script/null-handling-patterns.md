# Null Handling Patterns for Qlik Scripts

<!-- TODO: Populate with vCleanNull function pattern (parameterized SET variable for cleaning string-encoded nulls), NullAsValue patterns for sparse fields, Null() function usage, defensive null handling in every extraction, and string-null detection ('null', 'NaN', 'none', 'n/a', ''). -->
