# Source Profile Template

<!-- TODO: Populate with structured template for manual source profiling when MCP is unavailable. Should include sections for each source table: schema, field inventory (name, type, cardinality, null rate, sample values), data quality notes, architectural role classification, key structure, mutability, and incremental load pattern. -->
